/* import { LinksData, MetaData } from '@/types';

enum PaginationAction {
  NEXT = 'next',
  PREV = 'prev',
  FIRST = 'first',
  LAST = 'last',
  PAGE = 'page',
}

interface Params {
  links: LinksData
  meta: MetaData
  action: PaginationAction
  page?: number
}

function generatePaginationUrl({
  endpoint, links, meta, action, page,
}: Params) {
  const baseUrl = import.meta.env.VITE_API_BASEURL;
}

export default generatePaginationUrl; */
